# PS-03 Output Archive

Run literary regression and quality evaluation here with:

```bat
python launcher_translate.py regression --stage PS-03 --profile literary
```

Evaluate existing outputs only:

```bat
python launcher_translate.py evaluate --stage PS-03
```
