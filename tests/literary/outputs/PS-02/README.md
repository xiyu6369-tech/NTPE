# PS-02 Output Archive

Run literary regression outputs here with:

```bat
python launcher_translate.py regression --stage PS-02 --profile literary
```

Use `--dry-run` when validating packages only.
