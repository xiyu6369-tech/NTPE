# NTPE CLI Commands

```bash
ntpe version
ntpe doctor
ntpe translate <input> [--output output] [--resume] [--provider mock] [--quality standard]
ntpe project create|open|info|validate|list|export|import
ntpe benchmark run|runtime|provider|stress|report|compare
ntpe quality check|score|repair|report|rules
ntpe session create|list|info|resume|pause|stop|checkpoint|restore|cleanup
ntpe config list|get|set|validate|export|import|reset
ntpe plugin list|info|enable|disable|install|uninstall|validate
```
