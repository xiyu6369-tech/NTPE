# TE v4.3 Stage-4.3.1 Runtime Recovery Hook Contract

Defines the disabled-by-default, explicit opt-in, shadow-only contract for the
Translation Runtime recovery hook pilot.

No Provider Runtime, HTTP/client, API key, launcher, or production Translation
Runtime execution path is touched.
