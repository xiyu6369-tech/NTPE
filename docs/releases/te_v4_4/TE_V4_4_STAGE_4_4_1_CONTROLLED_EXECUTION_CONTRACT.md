# TE v4.4 Stage-4.4.1 Controlled Execution Contract

Defines a disabled-by-default, explicit-opt-in, single-chunk controlled recovery contract.

The original result remains preserved. Any replacement requires a separate guard decision. Real provider requests, provider fallback, real translation, and direct runtime mutation remain forbidden.
