# NTPE CLI Baseline v1

Baseline: `beta-1.0-cli`

This baseline is the official command-line contract used by Stage-07 SDK and later release-candidate work.

The CLI is considered frozen after Stage-06.9. New upper-layer functionality should build on the stable commands rather than replacing them.
